El proyecto cuenta con la **Fase 2 (Portal de Tesorería)** y la **Fase 5 (Integración WebView App Eclipse & Rediseño Multi-Factura/Cliente)** totalmente operacionales e implementadas en el código.

---

> **Nota sobre la Documentación:** Este documento (`PROJECT_STATUS.md`) refleja el estado operativo **actual** del proyecto (qué está terminado hoy y cuál es el bloqueo o próximo paso inmediato). Para ver la planificación a largo plazo, alcances detallados de cada fase y dependencias, consulte `ROADMAP.md`.

## Avance por Fases

| Fase | Descripción | Estado | Avance |
|------|-------------|--------|--------|
| **Fase 1** | Backend PHP: BD central, endpoints API, integración real con frontend | ✅ Completado | 100% |
| **Fase 2** | Portal de Tesorería (`/admin/`) con UI compacta, sub-filtros y tiempo relativo | ✅ Completado | 100% |
| **Fase 3** | Notificaciones por correo SMTP host y Portal Cuentas Corrientes | ✅ Completado | 100% |
| **Fase 4** | Motor de alertas por días transcurridos y Despacho Automático Diario (Cron Job) | ✅ Completado | 100% |
| **Fase 5** | Integración WebView App Android / Portal Vendedores Web & Multi-Factura | ✅ Completado | 100% |

---

## Componentes Entregados (Fase 1 y 2)

- [x] **Resolución de P0s — Validación Canónica de `vend_cod` y Suite HTTP Real de Integración (2026-09-04)** — Corrección exclusiva de puntos P0 críticos de la migración y handoff seguro:
  1. *P0-1 — Validación Estricta de `vend_cod` sin Coerción Implícita:* En SQL (`TRIM(vend_cod) REGEXP '^[1-9][0-9]*$'`) aplicado antes de cualquier CAST en `WebUsuariosSellerRepository` (`search` y `findById`) y en `SellerHandoffService::verifySessionToken`. En PHP, `validateVendCod` valida con `/^[1-9][0-9]*$/D` y rango signed INT (1 a 2147483647). Se descartan tajantemente: 0, negativos, signos, decimales, notación científica, caracteres alfanuméricos, espacios, desbordes y ceros a la izquierda (`0012` rechazado para mantener canonicidad única). Búsqueda exacta en SQL compara inequívocamente cadenas sin permitir coerción implícita de MySQL (`TRIM(vend_cod) = :vend_cod`). Certificado con 26 pruebas PASS en `scratch/test_seller_directory.php`.
  2. *P0-2 — Suite HTTP Real de Integración (`scratch/test_http_handoff.php`):* Servidor PHP CLI efímero arrancado en puerto libre con entorno protegido (`TEST_HTTP_SERVER=1`, `APP_ENV=local`). Prohibición absoluta de inyectar o ejecutar verificadores mock en `APP_ENV=production` (excepción inmediata). Certificado ciclo HTTP real completo:
     - POST real responde código **HTTP 303 (See Other)**.
     - `Location` fija y limpia hacia cheques (`/form/index.html`) y rendiciones (`/form/rendiciones/vendedor.php`). Cero fuga de tokens, vendedor_id, nombre, email o empresa en la URL o query string.
     - Emisión de cookie segura de vendedor `AUTOMARCO_SELLER_SID`.
     - Regeneración obligatoria de Session ID comprobada (mitigación de fijación de sesión).
     - Segunda petición HTTP GET con la cookie a `api/auth/session_vendedor.php` responde 200 OK y recupera el contexto oficial autenticado.
     - Reutilización exitosa de la sesión comercial vigente (sin bloqueo artificial).
     - Validación de códigos de error: GET responde 405; token inválido responde 401; destino no autorizado responde 400; Origin no permitido responde 403.
     - Cero fugas: ninguna cabecera, URL ni cuerpo expone el token comercial.
  3. *Transaccionalidad Estricta y Cero Datos Residuales:* Toda escritura de auditoría en pruebas (`SELLER_HANDOFF`) se ejecuta dentro de una transacción en `bd_modulo_cobranzas` y se revierte automáticamente mediante `ROLLBACK` en el shutdown handler. Cero registros residuales de auditoría en la BD central (antes y después idénticos). Cero `DELETE` físicos.
  4. *Cero Escrituras en Bases ERP:* Certificado que las cuatro bases de datos ERP (`automarc_automarco`, `autohd_automarcohd`, `autotec_ecom`, `gabteccl_sitbdd1978`) se mantienen 100% de solo lectura.
  5. *Paridad 100% SHA-256:* Sincronizados los servicios a `dist/cheques_cobranza/app/`, certificando 235 archivos idénticos en `scripts/verify_release.php`.
- [x] **Migración a `web_usuarios` y Handoff Seguro por POST (2026-09-04)** — Arquitectura desacoplada de identidad comercial y transición segura entre portales:
  1. *Directorio de Vendedores con Adaptadores ERP:* `ErpSellerDirectoryService` refactorizado con arquitectura de repositorios (`WebUsuariosSellerRepository` para Automarco, Autotec y Gabtec; `LegacySellerRepository` como integración temporal para Automarco HD). Consultas 100% preparadas y estrictamente de solo lectura sobre las cuatro bases ERP.
  2. *Tolerancia de Vendedores sin Correo Corporativo:* Soporte completo para vendedores con `vendedor_email = null` (ej. 100% de los vendedores en Autotec). En la interfaz administrativa "Vendedores y presupuestos" (`admin/js/rendiciones.js`), se rotula limpiamente como "Sin correo corporativo" y se permite la creación de presupuestos mensuales y giras sin forzar emails ficticios.
  3. *Validación Estricta de Identidad ERP:* Validación de `vend_cod` entero positivo (`> 0`), filtro `rol = 'vendedor'`, `activo = 1`, y detección automática de ambigüedad (`DomainException` ante colisiones de código dentro de la misma razón social). Revalidación obligatoria de `empresa_id` + `vendedor_id` en `admin/api/rendiciones/gestion_presupuestos.php` descartando nombres o correos manipulados por el cliente.
  4. *Handoff Web Seguro por POST (`api/seller_handoff.php`):* Endpoint dedicado que recibe exclusivamente por `POST` (`session_token`, `empresa`, `destino`). Valida el destino contra lista cerrada (`cheques` hacia `/index.html`, `rendiciones` hacia `/rendiciones/vendedor.php`), verifica la sesión activa en modo lectura contra `web_sesiones JOIN web_usuarios` de la BD ERP, regenera el session ID (`session_regenerate_id(true)`), crea o renueva el token CSRF, y redirige mediante código **HTTP 303 (See Other)** sin exponer tokens, identidades ni datos en la URL ni en cabeceras `Location`.
  5. *Reutilización Idempotente de Sesión Comercial:* El mismo token comercial activo puede reutilizarse en múltiples ocasiones para acceder a Cheques o Rendiciones sin generar bloqueos artificiales ni registros de un solo uso.
  6. *Auditoría Segura sin Fuga de Credenciales:* Evento `SELLER_HANDOFF` registrado en `audit_logs` con `empresa_id`, `vendedor_id`, `destino` y resultado `SUCCESS`, sin almacenar jamás el token crudo ni hashes reutilizables.
  7. *Bloqueo de Acceso Legacy en Producción:* En `APP_ENV=production`, `api/auth_seller.php` bloquea inmediatamente (HTTP 401) la autenticación basada en `vendedor_id`, `vendedor`, `vendedor_nombre` o `empresa` pasados por query string en la URL. En `APP_ENV=local`, se preserva exclusivamente para agilizar el desarrollo local.
  8. *Portales Web Comerciales Actualizados:* `vendedores/pages/cobranza.html` y `vendedores_DEV/pages/cobranza.html` actualizados con `enviarHandoffSeguro(destino)` mediante formulario invisible dinámico, eliminando parámetros de identidad de los enlaces y sin emojis en la UI.
  9. *Aclaración de Alcance:* La aplicación Android (`android/Autotec_Grande`) queda explícitamente fuera de alcance, intacta y no representa un bloqueo para el despliegue a producción de los portales web.
  10. *Certificación QA Exhaustiva:* 26 pruebas de directorio PASS (`test_seller_directory.php`), 24 pruebas de handoff PASS (`test_seller_handoff.php`), 25 pruebas HTTP de integración PASS (`test_http_handoff.php`), 148 comprobaciones de rendiciones PASS (`test_rendiciones.php`), 40 comprobaciones de workflow PASS (`test_approval_workflow.php`), cero schema drift (`test_clean_setup.php`) y paridad 100% SHA-256 (235 archivos idénticos en `verify_release.php`).
- [x] **Cierre Local al 100% — Correos a Vendedores Deshabilitados, Inmutabilidad e Idempotencia Certificada (2026-09-04)** — Certificación integral y remediación completa del entorno local:
  1. *P0 — Política de Correos a Vendedores Deshabilitada:* Por decisión de negocio, las notificaciones a vendedores están estrictamente deshabilitadas en todos los entornos (`MAIL_SELLER_NOTIFICATIONS_ENABLED = false` por defecto). Correos internos a aprobadores, Tesorería, CC y digitadoras continúan activos en producción. Omisión segura en logs sin datos sensibles (sin email, token, link ni HTML) y retorno controlado `true`.
  2. *P1 — Inmutabilidad Real de Cheques Dados de Baja:* Blindadas todas las rutas operacionales de `UPDATE cheques` y `SELECT` (`api/editar_cheques.php`, `admin/api/cambiar_estado.php`, `admin/api/editar_cobranza_tesoreria.php`) exigiendo `AND activo = 1` o `AND (activo = 1 OR activo IS NULL)`. Un cheque dado de baja no cambia monto, foto, fecha ni papeleta de depósito, no suma en totales y no puede reactivarse. Resolución de actor en `descartado_por` valida existencia en `usuarios(id)`; códigos ERP no presentes en usuarios usan fallback seguro a `1` (Sistema) preservando el actor ERP en el motivo y bitácora.
  3. *P1 — Migraciones Idempotentes y Selección de BD:* Todas las migraciones en `config/migrations/` incorporan `USE \`bd_modulo_cobranzas\`;`, procedimientos temporales con DROP antes/después y guardas dinámicas `information_schema`. Certificada doble ejecución consecutiva sin errores en base temporal aislada. Se documenta explícitamente que las migraciones pendientes todavía no deben ejecutarse en producción.
  4. *P2 — Hardening de `scratch/test_clean_setup.php`:* Generación de sufijo con `random_bytes()`, aborto seguro sin eliminación si la BD existe, eliminación en `finally` condicionada a creación por la ejecución actual, y comparador estructural exhaustivo de schema drift (17 tablas, columnas, orden, tipos, nullability, defaults, extras, generated expressions, índices, unicidad, foreign keys con ON UPDATE/DELETE y restricciones CHECK).
  5. *P2 — Ampliación de QA en `scripts/verify_release.php`:* Verificación de 10 contratos estrictos: sintaxis PHP (109 archivos), contrato de esquema (17 tablas), Zero Delete, Named Parameters, Error Sanitization, Inmutabilidad de Cheques (56 archivos backend), política de correos a vendedores deshabilitada, migraciones idempotentes con BD explícita, schema drift exhaustivo, frontend sin emojis (37 archivos) y paridad SHA-256 exacta (233 archivos idénticos).
- [x] **Corrección Integral Preproducción — Seguridad, Zero Delete, Migraciones y QA (2026-09-04)** — Remediación exhaustiva de seguridad, integridad financiera y verificación de release:
  1. *Zero Delete en Cheques:* Reemplazado `DELETE FROM cheques` por baja lógica aditiva (`activo = 0`, `descartado_at = NOW()`, `descartado_por = :user_id`, `motivo_descarte = :motivo`) en `api/editar_cheques.php`. Eliminada la destrucción prematura de fotos físicas en disco (`unlink`), preservando las imágenes para su ciclo de purga cronológica (>3 meses). Incorporado filtro retrocompatible `AND (activo = 1 OR activo IS NULL)` en todas las consultas y subconsultas del sistema (`admin/api/get_cobranzas.php`, `get_detalle_cobranza.php`, `get_gestion_cc.php`, `cambiar_estado.php`, `despachar_resumen_cc.php`, `reenviar_informe_cc.php`, `completar_envio.php`, `services/PdfGenerator.php`, `services/MailService.php`, `cron/resumen_diario_cuentas_corrientes.php`).
  2. *Migración Aditiva e Idempotente de Cheques:* Creado `config/migrations/2026_09_04_baja_logica_cheques.sql` y sincronizado `config/setup.sql` con las 4 columnas de baja lógica, índices `idx_cheques_activo`, `idx_cheques_descartado_por` y clave foránea `fk_cheques_descartado_usuario`.
  3. *Erradicación Total de Placeholders Posicionales (`?`):* Migradas al 100% todas las consultas preparadas restantes hacia parámetros nombrados (`:param`) en `PdfGenerator.php`, `MailService.php`, `api/get_mis_cobranzas.php`, `cron/resumen_diario_cuentas_corrientes.php` y `cron/check_alertas.php`.
  4. *Sanitización Estricta de Errores y Excepciones:* Eliminadas todas las filtraciones de `$e->getMessage()`, rutas físicas del servidor y trazas de error en respuestas HTTP 500 (`api/guardar_cobranza.php`, `api/completar_envio.php`, `api/get_factura.php`, `api/get_mis_cobranzas.php`, `api/get_facturas_cliente.php`, `api/get_clientes.php`, `api/auth/login.php`, `api/editar_cheques.php`). Todos los errores se canalizan exclusivamente a `error_log()`.
  5. *Diagnóstico Preflight con `SIGNAL SQLSTATE '45000'` en Migraciones:* En `config/migrations/2026_09_02_indices_y_fks_auditoria.sql`, se reemplazaron las sentencias destructivas `UPDATE ... SET ... = NULL` por procedimientos almacenados temporales de diagnóstico que abortan la migración con error explícito si existen IDs huérfanos antes de aplicar las claves foráneas.
  6. *Hardening de Prueba de Instalación Limpia:* `scratch/test_clean_setup.php` endurecido con nombres aleatorios de base de datos (`bd_modulo_cobranzas_qa_<hash>`), validación estricta de prefijos y verificación explícita de las nuevas columnas de cheques, confirmando cero schema drift contra la BD central.
  7. *Erradicación Total de Emojis en Frontend:* Limpiados todos los emojis y dingbats en `admin/admin.js`, `admin/detalle.php`, `admin/index.php`, `admin/js/cuentas_corrientes.js`, `rendiciones/aprobar_gira.php`, `rendiciones/aprobar_rendicion.php`, `rendiciones/vendedor.php`, `index.html` y `script.js`, reemplazándolos por SVGs vectoriales, entidades HTML o etiquetas neutras profesionales.
  8. *Blindaje de Queries Cross-DB con Backticks:* En `api/get_clientes.php` y `api/get_facturas_cliente.php`, los esquemas cruzados dinámicos (`{$db_origen}`) se envuelven estrictamente con backticks.
  9. *Verificador Global de Despliegue (`scripts/verify_release.php`):* Suite de verificación estática expandida para auditar sintaxis PHP (109 archivos), contrato de esquema (17 tablas), contrato de seguridad global (56 archivos backend sin `?`, sin `DELETE` en tablas de negocio y con sanitización de errores), contrato de frontend (37 archivos sin emojis) y paridad SHA-256 exacta (233 archivos idénticos).
  10. *Checklist de Migraciones de Producción:* Creado `docs/CHECKLIST_MIGRACIONES_PRODUCCION.md` con el procedimiento paso a paso para DBA vía phpMyAdmin y consultas de verificación post-migración.
- [x] **Cierre Final Pre-Producción — Rendiciones de Gastos (2026-09-02)** — Cierre integral del Módulo de Rendiciones previo al paso a producción:
  1. *Alineación Total de Esquema y DDL (`setup.sql` y `setup_rendiciones.sql`):* Sincronizadas las 17 tablas centrales con sus definiciones productivas: `rendiciones_gastos` (`PENDIENTE_APROBACION_RESPONSABLE`, `verificado_tesoreria_at`, `verificado_tesoreria_por`, `pdf_planilla_url`, índices y FKs), `rendicion_documentos` (`numero_documento_original`, `monto_original`, `editado_por`, `editado_at`, `motivo_edicion`, `document_hash_bloqueante` STORED GENERATED, `uq_rendicion_document_hash_bloqueante`, índices y FKs), `solicitudes_aprobacion` (`APROBACION_RENDICION`, `APROBADA_TOPE`, `chk_solicitud_objetivo`).
  2. *Migración Idempotente para Bases Existentes:* Creado `config/migrations/2026_09_02_indices_y_fks_auditoria.sql` con limpieza preventiva de registros huérfanos y creación segura con `information_schema` de columnas, índices y claves foráneas.
  3. *Preservación Inmutable de Folio Original (`numero_documento_original`):* En `admin/api/rendiciones/corregir_documento.php` y `get_detalle_rendicion.php`, la regla `COALESCE(numero_documento_original, numero_documento)` preserva el folio primigenio digitado por el vendedor ante correcciones administrativas sucesivas o de monto.
  4. *Auditoría Individual Append-Only por Comprobante en `VALIDAR_DOCUMENTOS`:* En `services/RendicionesService.php`, cada comprobante genera un evento en `rendicion_historial_estados` con acción `VALIDAR_DOCUMENTO`, enlazando `documento_id`, estados anterior/nuevo, montos anterior/nuevo, decisión, motivo y folios, con rollback transaccional completo ante errores.
  5. *Aislamiento Seguro de Artefactos QA y Limpieza en Disco:* Propiedad `RendicionPlanillaPdf::$testUploadDir` restringida a modo CLI (`php_sapi_name() === 'cli'`) y limpieza en `finally`. Cero archivos PDF residuales de prueba en `uploads/`.
  6. *Instalación Limpia y Validación Anti-Schema Drift:* Script automatizado contra base temporal `bd_modulo_cobranzas_qa_setup` con validación estricta de `SELECT DATABASE()`. Confirmadas las 17 tablas centrales y cero schema drift contra la BD local migrada.
  7. *Batería de 148 Comprobaciones Automatizadas:* 148 comprobaciones PASS en `scripts/test_rendiciones.php` (incorporando los 15 casos obligatorios de cierre) y 40 comprobaciones PASS en `scripts/test_approval_workflow.php`.
  8. *Paridad 100% SHA-256:* Sincronización exacta en `dist/cheques_cobranza/app/`, certificada por `scripts/verify_release.php` (232 archivos auditados).
- [x] **Reutilización Controlada de Boletas Rechazadas y Endurecimientos P1 (2026-09-02)** — Implementación completa de la política de reutilización y blindaje P1:
  1. *Regla de Negocio para Duplicados e Índice Único MySQL 8.4.3:* Reemplazado `uq_rendicion_document_hash` por la columna generada STORED nullable `document_hash_bloqueante` y el índice único `uq_rendicion_document_hash_bloqueante`. Estados `BORRADOR`, `PENDIENTE` y `APROBADO` bloquean duplicados en todo el holding (incluso con rendición `PAGADA`). Estados `RECHAZADO` y `DESCARTADO` generan NULL en la columna generada, permitiendo al vendedor volver a presentar el comprobante en una nueva rendición con registro independiente y sin mutar el histórico (Zero Delete).
  2. *Carga de Documentos del Vendedor (`api/rendiciones/guardar_documento_bolsa.php`):* Pre-verificación SELECT de estados bloqueantes y captura atómica de `isDuplicateKey`, respondiendo en ambos casos HTTP 409 con el mensaje controlado `"Este comprobante ya fue registrado en una rendición activa, aprobada o pendiente."`.
  3. *Corrección de Folio en Revisión (`admin/api/rendiciones/corregir_documento.php`):* Validación previa contra estados bloqueantes (`BORRADOR`, `PENDIENTE`, `APROBADO`) respondiendo HTTP 409 controlado ante colisión. Permite corrección si el folio solo coincide con registros en `RECHAZADO` o `DESCARTADO`.
  4. *Endurecimiento P1 en Aprobación con Tope (`services/ApprovalWorkflowService.php`):* En `resolveByToken()` y `applyRenditionApproval()`, se eliminó definitivamente el fallback `$baseDocs`. Se valida antes de tocar la solicitud o consumir el token que `monto_maximo_aprobable > 0`, `totalDocsAprobados > 0` y `cappedAmount > 0`. Si falla, lanza `DomainException` sin consumir el token (`token_usado_at` NULL, solicitud en `PENDIENTE_DECISION`). Cálculo estricto: `$cappedAmount = min($maxAprobable, $totalDocsAprobados, $totalRendido)`.
  5. *Endurecimiento P1 en Estados Documentales (`services/RendicionesService.php`):* En `verificarYEnviar()`, se comprueba estrictamente que cada documento activo esté en `['APROBADO', 'RECHAZADO']`. Bloquea con `DomainException` ante `BORRADOR`, `DESCARTADO`, `PENDIENTE` o desconocido. Exige al menos un aprobado y suma validada > 0.
  6. *Migración SQL Independiente:* Creado `config/migrations/2026_09_02_reutilizacion_documentos_rechazados.sql` para ejecución manual en phpMyAdmin.
  7. *Batería Completa de Pruebas Automatizadas:* 133 comprobaciones en `scripts/test_rendiciones.php` y 40 comprobaciones en `scripts/test_approval_workflow.php`, cubriendo los 23 casos mínimos obligatorios y el ciclo completo con rollback.
- [x] **Corrección Urgente P0: Blindaje del Flujo de Aprobación de Rendiciones (2026-09-02)** — Blindaje integral de tres vulnerabilidades críticas en el ciclo de aprobación gerencial y documental:
  1. *Eliminación del Bypass en `VERIFICAR_Y_ENVIAR`:* Las decisiones documentales sólo se procesan por la ruta canónica `VALIDAR_DOCUMENTOS`. `VERIFICAR_Y_ENVIAR` rechaza con HTTP 422 si recibe el campo `decisiones`. Comprueba obligatoriamente que todos los comprobantes activos estén resueltos como `APROBADO` o `RECHAZADO` (sin comprobantes `PENDIENTE`), que exista al menos un comprobante aprobado y que el total validado sea mayor a cero. Se eliminó completamente el fallback que sustituía un total validado de cero por el `monto_total_rendido`.
  2. *Validación Estricta de Decisiones Documentales (`RendicionesService::validarDocumentos`):* Acepta exclusivamente `APROBAR` o `RECHAZAR` (cualquier otro valor o valor ausente responde HTTP 422). Sin `continue` silencioso: rechaza IDs ausentes, inexistentes, de otra rendición, duplicados en payload, montos validados menores o iguales a cero, montos superiores al rendido original y rechazos sin motivo. Recalcula atómicamente reservas y presupuesto en orden FIFO bajo bloqueos `FOR UPDATE`, manteniendo la rendición en `EN_REVISION_TESORERIA`.
  3. *Blindaje de `APROBADA_TOPE` (Tope Cero y Límites):* En `services/ApprovalWorkflowService.php`, se eliminó el fallback donde un tope presupuestario <= 0 aprobaba la totalidad rendida. Si `monto_maximo_aprobable <= 0`, lanza `DomainException` antes de tocar la solicitud, garantizando que el token NO se consuma (`token_usado_at` nulo, estado `PENDIENTE_DECISION`). El monto aprobado se calcula como $\min(\text{max\_aprobable}, \text{total\_docs\_aprobados}, \text{total\_rendido})$, guardando el exceso en `monto_exceso_no_reembolsable`.
  4. *Cancelación Auditable en Rechazo de Tesorería:* Mediante `ApprovalWorkflowService::cancelRequest($pdo, $solicitudId, $actor, $motivo, false)`, el rechazo directo de Tesorería cancela quirúrgicamente la solicitud abierta vinculada en `solicitud_excepcion_id` perteneciente a la rendición, dejando intactas solicitudes históricas ya resueltas y registrando el evento en `solicitud_aprobacion_historial`. Se libera siempre exactamente la reserva almacenada en `monto_maximo_aprobable`.
  5. *Suite de Pruebas Automatizadas:* 104 comprobaciones superadas en `scripts/test_rendiciones.php` (cubriendo explícitamente los 22 casos obligatorios) y 38 comprobaciones superadas en `scripts/test_approval_workflow.php`.
  6. *Paridad 100% SHA-256:* Replicados todos los archivos a `dist/cheques_cobranza/app/`, certificando 230 archivos idénticos vía `scripts/verify_release.php`.
- [x] **Corrección Urgente P0: Integridad Financiera y Aprobación Obligatoria** — Implementada la remediación P0 integral de los 3 vectores de riesgo financiero y aprobación:
  1. *Ajuste de Presupuesto en Corrección Documental (`admin/api/rendiciones/corregir_documento.php`):* El impacto en `monto_utilizado` se calcula estrictamente con base en la diferencia de reservas (`reserva_nueva - reserva_anterior`), donde `reserva = min(total_rendido, saldo_disponible_al_enviar)`. Bloqueos `FOR UPDATE` en cascada (documento → rendición → presupuesto), eliminación de SQL dinámico/interpolación y trazabilidad en historial y `audit_logs`.
  2. *Liberación de Fondos en Rechazo (`services/ApprovalWorkflowService.php`, `admin/api/rendiciones/cambiar_estado.php`):* Al rechazar una rendición (gerencial o por Tesorería), se libera siempre exactamente `monto_maximo_aprobable` con `GREATEST(0, monto_utilizado - :monto)`. Al rechazar directamente por Tesorería, se cancela quirúrgicamente únicamente la solicitud abierta vinculada en `solicitud_excepcion_id` perteneciente a la rendición, sin alterar solicitudes resueltas.
  3. *Erradicación del Bypass de Aprobación Gerencial:* Bloqueo estricto con HTTP 409 de `APROBAR_TOTAL` y `APROBAR_PARCIAL` en backend; la transición a `APROBADA` solo puede ocurrir mediante `ApprovalWorkflowService::resolveByToken()`. Incorporada la acción `VALIDAR_DOCUMENTOS` que recalcula montos validados, reserva, exceso y presupuesto en la misma transacción manteniendo la cabecera en `EN_REVISION_TESORERIA`. Estados `DOCUMENTOS_FISICOS_RECIBIDOS` y `APROBADA_PARCIAL` conservados sólo para compatibilidad histórica. Auditoría de `APROBADA_TOPE` corregida a `APROBAR_SOLICITUD_HASTA_TOPE`.
  4. *Erradicación de Emojis en Frontend:* Sustituidos todos los emojis de `admin/rendiciones.php`, `admin/js/rendiciones.js` y `rendiciones/vendedor.js` por SVG inline vectoriales y etiquetas profesionales.
  5. *Suite de Pruebas Automatizadas con Transacciones y ROLLBACK:* Implementados los 10 casos de prueba obligatorios con dos conexiones PDO independientes para concurrencia (`scripts/test_rendiciones.php` con 80 checks PASS y `scripts/test_approval_workflow.php` con 35 checks PASS).
  6. *Paridad 100% SHA-256 en `dist/`:* Replicados todos los archivos a `dist/cheques_cobranza/app/`, certificando 230 archivos con SHA-256 idéntico vía `scripts/verify_release.php`.
- [x] **Flujo Integral de Aprobación con Responsable y Planilla PDF Tipo Excel** — Implementado flujo integral donde Tesorería revisa las fotos de las boletas contra lo digitado y cuenta con edición directa de comprobantes (con auditoría de monto original, usuario, fecha y motivo). El botón de aprobación de Tesorería ejecuta "Verificar y Enviar a Responsable", permitiendo escoger cuál de los responsables autorizará la rendición. Se despacha correo con Magic Token unificando el desglose de comprobantes y la autorización de exceso si aplica. Al resolver el Magic Link, la rendición pasa a estado `APROBADA` y genera automáticamente la Planilla Oficial en PDF formato Excel apaisado (`services/RendicionPlanillaPdf.php`, A4 Horizontal con cuadrícula, detalle completo, antecedentes SII, subtotales por categoría y 3 firmas: Vendedor, Tesorería y Aprobador). Tesorería puede marcar como `PAGADA` sin requerir recepción física previa.
- [x] **Flujo de 3 Opciones de Tesorería ante Excesos** — En el panel de revisión de rendiciones con exceso, Tesorería dispone de 3 vías de resolución inmediata: (1) Aprobar hasta el tope del presupuesto ordinario con liquidación FIFO y confirmación clara, (2) Solicitar aprobación de exceso a Gerencia mediante Magic Link con token SHA-256 de 48 horas a los responsables configurados, y (3) Rechazar la rendición con motivo obligatorio y liberación íntegra de fondos comprometidos. Se habilitó la transición directa desde `EN_REVISION_TESORERIA` a `APROBADA` sin forzar la recepción física previa.
- [x] **Comprobante PDF de Acreditación de Giras Comerciales** — Implementado generador institucional en FPDF (`services/GiraApprovalPdf.php`) y endpoint de descarga/impresión (`admin/reportes/comprobante_aprobacion_gira.php`). El comprobante acredita la autorización gerencial con identificación de la gira, fechas de vigencia, vendedor y empresa, justificación comercial, resumen financiero, firma y código de verificación SHA-256 inmutable. Integrado en la tabla de Vendedores y Presupuestos (`admin/js/rendiciones.js`) mediante botón "Certificado PDF" en filas de giras con estado `APROBADA`.
- [x] **Adaptación Tablet y Responsive Portal Vendedor** — Reestructuración unificada de media queries a partir de `min-width: 640px` (`rendiciones/vendedor.css`), eliminando bloqueos de 480px en tablets (iPad, Surface Pro) y adaptando tarjetas a contenedor amplio de 940px. Incorporado cachebuster dinámico (`filemtime`) en `rendiciones/vendedor.php` para invalidación instantánea de caché en navegadores cliente.
- [x] **Topes y aprobaciones — Fases A–G** — Esquema aditivo, reserva hasta el tope, liquidación FIFO, excepción mensual opcional y aprobación previa de giras integrados mediante `ApprovalWorkflowService`. El modal exige justificación y responsable; reenvío/cambio/cancelación rotan tokens y registran el resultado del correo. Aumentar una gira aprobada exige nueva autorización. QA local acumulado: 34 pruebas transaccionales + 38 pruebas funcionales PASS. Para desplegar se debe importar primero `config/migrations/2026_08_28_topes_y_flujo_aprobaciones.sql`.
- [x] **Topes y aprobaciones — Fases H/I local** — Dashboard incorpora estado de solicitudes gerenciales, correos fallidos, tasa y tiempo de decisión, antigüedad y desglose estandarizado Gira/Excepción. Certificación local: 34 pruebas transaccionales y 38 funcionales con rollback, HTTP de endpoints/páginas, login y CSRF, sintaxis PHP/JS y paridad root/dist. El único ensayo externo pendiente es SMTP productivo con destinatarios de prueba autorizados.
- [x] **Sesiones resilientes Admin/Vendedor** — Cookies PHP separadas por contexto, TTL físico coherente, heartbeat, recuperación silenciosa tras `401`, persistencia temporal de identidad multiempresa y retorno seguro al módulo administrativo solicitado. El token del portal comercial se renueva durante actividad. SQL nuevo: ninguno.

- [x] **Logo oficial en Shell ERP** — La cabecera compartida de Cheques, Cuentas Corrientes, Rendiciones y Usuarios utiliza el recurso oficial del holding desde la raíz de la aplicación, manteniendo altura compacta y adaptación móvil.

- [x] `config/setup.sql` — Script DDL completo y alineado con columnas de seguridad (`token_expires_at`).
- [x] `config/app.php` — Constantes de entorno y configuración.
- [x] `config/db.php` — Clase Database PDO.
- [x] `config/auth.php` — Middleware de autenticación (`requireAuth`, control de fuerza bruta y resolución de IP real).
- [x] `api/get_factura.php` — Endpoint de búsqueda de facturas ERP con blindaje de backticks en interpolación de BD.
- [x] `api/guardar_cobranza.php` — Endpoint transaccional de cobranzas.
- [x] `api/get_mis_cobranzas.php` — Historial de cobranzas del vendedor ordenable por última modificación.
- [x] `api/auth/login.php` — Autenticación segura con hasheo de tokens, expiración y control de intentos fallidos.
- [x] `services/AuditService.php` — Bitácora de auditoría transaccional.
- [x] `api/completar_envio.php` — Segundo paso del vendedor asegurado contra IDOR y bypass en local.
- [x] `admin/index.php` & `admin/admin.js` — Portal web de Tesorería con sub-filtros dinámicos, columna de tiempo relativo y botón de cierre visible.
- [x] `admin/login.php` — Formulario de acceso seguro para el portal de Tesorería.
- [x] `admin/api/cambiar_estado.php` — Cambio de estado transaccional y auditado con RBAC estricto.
- [x] `services/MailService.php` — Implementación de cliente SMTP por sockets robusto con adjuntos de cheques.
- [x] `docs/ANDROID_INTEGRATION.md` — Documentación y especificación técnica de la App Android (Eclipse) y flujo Vendedor ➔ Cliente ➔ Multi-Factura.
- [x] Actualización de Reglas de Negocio (Cheques & Descuadre): Traslado de digitación de Banco/Cheque hacia Tesorería (`modalCompletarCheques`) y requerimiento obligatorio de texto de justificación (`justificacion_descuadre`) en caso de diferencias de montos.
- [x] `admin/api/editar_cheques.php` — API para corrección y modificación manual de cheques por parte de Tesorería.
- [x] Fragmentación Dinámica de Despachos — El botón "Despachar Resumen" ahora divide las cobranzas según el campo `emitido_a` de cada cheque y distribuye correos distintos a múltiples digitadoras simultáneamente con asuntos categorizados.
- [x] Refactorización Visual de Facturas (`admin.js`) — Agrupación estética de facturas múltiples con cuotas en el detalle del portal de Tesorería.
- [x] `services/GoogleSheetsService.php` — Integración ultra-liviana con la API v4 de Google Sheets (autenticación JWT nativa con `openssl_sign` y cURL sin Composer) que inserta automáticamente cada cheque validado al Excel corporativo de Tesorería.
- [x] Re-validación Backend de Saldos (`SEC-04`): Implementado escudo protector en `api/guardar_cobranza.php` que consulta `bd_automarco.tbl_cobranza` antes de guardar, bloqueando cualquier manipulación o sobrepago fraudulento de saldos desde el navegador.
- [x] Potenciación de Bitacora y Trazabilidad (`admin/cuentas_corrientes.php`) — Inserción de la columna "Clientes Afectados" en el historial de despachos e inspección estructurada de cheques digitalizados/registrados (banco, número, monto y vencimiento) para control y auditoría de extravíos sin imágenes.
- [x] **Unificación y Sincronización `dist/`** — Consolidación del 100% de los archivos entre la raíz del proyecto y la carpeta `dist/` (hashes MD5 idénticos), dejándola en un estado 100% listo para despliegue directo en producción.
- [x] **Parche de Integridad ERP (Foreign Keys)** — Solucionada incompatibilidad de `vendedor_id` nativo del ERP en `api/guardar_cobranza.php`, `api/completar_envio.php` y `api/editar_cheques.php`, usando fallback a Usuario Sistema (`ID 1`) en `historial_estados` cuando el ID del vendedor no existe en la tabla de usuarios web local.
- [x] **Trazabilidad de Nombre Vendedor** — Ajustada la prioridad en `get_cobranzas.php` y `get_detalle_cobranza.php` para dar precedencia absoluta a `c.vendedor_nombre` capturado desde terreno.
- [x] **Sincronización Google Sheets** — Inyección automática de `WEB#{cobranza_id}` en la Columna G (Nº Recibo) de los 4 excels y adición de `BCIPREMIER` a la lista de bancos.
- [x] **Auditoría Pre-Despliegue AWS** — Revisión de seguridad y variables de entorno para Producción (`PORTAL_BASE_URL` a nivel raíz, `.htaccess` configurado, validación de rutas relativas de API).
- [x] **Hardening de Seguridad HTTP (OWASP ZAP)** — Implementación de CSP balanceada y compatible (`style-src`/`font-src` Google Fonts y data/blob), HSTS con `preload` y `includeSubDomains`, Permissions-Policy, X-Frame-Options, X-Content-Type-Options y mitigación de fugas de servidor.
- [x] **Blindaje de Directorio de Uploads** — Creación de `.htaccess` en `uploads/` y `dist/uploads/` con desactivación de motores PHP y bloqueo total de scripts ejecutables (`RCE Protection`).
- [x] **Integridad de Fuentes Web** — Enlace directo a Google Fonts (`Outfit` y `Plus Jakarta Sans`) en todas las vistas HTML/PHP y eliminación de imports locales inexistentes en `styles.css`.
- [x] **Fragmentación y Despacho Multi-Empresa de Cheques Físicos** — Inclusión de `emitido_a` y `cuenta_corriente` en las consultas de `get_detalle_cobranza.php` y `get_cobranzas.php`, sanitización estricta de empresa en `cambiar_estado.php`, pre-selección y validación obligatoria en modal de Tesorería (`admin.js`), badges de auditoría en drawer y fragmentación exacta en la Matriz de Despacho de Cuentas Corrientes (`cuentas_corrientes.js`, `despachar_resumen_cc.php`, `cron/resumen_diario_cuentas_corrientes.php`).
- [x] **Visualización de Cheque Digitalizado en Cuentas Corrientes** — Integración de miniaturas de cheques digitalizados y Visor Lightbox interactivo (zoom, rotación, alto contraste y descarga) en los modales de Detalle de Cobranza e Historial de Despachos y Trazabilidad (`cuentas_corrientes.js` y `get_gestion_cc.php`).

- [x] **Remediación OWASP ZAP (Erradicación `unsafe-inline`)** — Desacoplamiento total de código JS y estilos CSS inline en `dist/admin/` a archivos dedicados (`dist/admin/css/` y `dist/admin/js/`), permitiendo una CSP estricta `script-src 'self'` y `style-src 'self' https://fonts.googleapis.com` sin alertas Medium.
- [x] **Emisión Dual de Headers HSTS y Supresión Server** — Blindaje en `.htaccess` y en `config/app.php` de `Strict-Transport-Security`, `X-Content-Type-Options`, `X-Frame-Options` y eliminación de firmas `Server`/`X-Powered-By`.
- [x] **Integración Portal Vendedores Web ↔ Cobranza Cheques** — Implementado botón de recaudación en `dist/vendedores/pages/cobranza.html` consumiendo el `vend_cod` de la sesión web activa y transfiriendo contexto (`vendedor_id` + `empresa`) vía URL canónica hacia `cobranza_cheques/index.html`. Documentación completa en `docs/INTEGRATION.md`.
- [x] **Blindaje Integral contra IDOR en Producción (`SEC-01`) & Limpieza de URL** — Implementada validación forzada de identidad desde `$_SESSION['vendedor_auth']` en los 5 endpoints de vendedor (`get_clientes.php`, `get_facturas_cliente.php`, `guardar_cobranza.php`, `get_mis_cobranzas.php`, `completar_envio.php`), verificación de pertenencia a cartera ERP en `get_facturas_cliente.php` y eliminación estética y segura de query params en el navegador (`history.replaceState`). Sincronización dual completa en `dist/`.
- [x] **Cron Jobs de Producción: Despacho Automático CC & Motor de Alertas (`Fase 4`)** — Implementación robusta de `cron/resumen_diario_cuentas_corrientes.php` (despacho automático diario con hora de corte configurable en BD, fragmentación por empresa y control anti-duplicados) y `cron/check_alertas.php` (motor de alertas por días transcurridos para cobranzas demoradas con notificación a Vendedor, Tesorería y CC). Ambos protegidos con validación CLI / token web (`CRON_SECRET_KEY`) y documentados en `cron/README.md`.
- [x] **CRON de Purga Segura de Fotos Post-Vencimiento (>3 Meses)** — Implementación del script de mantenimiento preventivo `cron/purgar_fotos_cheques_vencidos.php` (y su réplica dual en `dist/`) para liberación automática de espacio en disco. Elimina físicamente fotos de cheques y comprobantes de cobranzas cuyos cheques tengan más de 3 meses de vencimiento, manteniendo el 100% de la integridad de datos en MySQL (`ZERO DELETE`, solo actualiza URLs a `NULL` y marca `foto_purgada_at`/`comprobante_purgado_at`), con protección estricta anti Path-Traversal (`realpath`), soporte para simulación `--dry-run`, procesamiento en bloques de 200 y registro de auditoría.
- [x] **Fase 1: Conversión a SaaS Shell ERP Modular (Zero-Breakage)** — Implementación del App Switcher unificado (`admin/includes/app_header.php`, `admin/css/shell.css`), centralización del visor Lightbox, Toasts y modales (`admin/js/shared_ui.js`), creación del stub preparado para el Módulo 3 (`admin/rendiciones.php`), eliminación de código duplicado en `admin.js` y `cuentas_corrientes.js`, y sincronización dual exacta de todos los archivos en `dist/cheques_cobranza/app/admin/`.
- [x] **Consolidación RBAC y Modo Administrador (Suite ERP)** — Matriz central de permisos en `config/auth.php`, sesión administrativa revalidada contra BD (email, actividad, rol y estado), hardening CSRF, acceso de consulta para Tesorería en CC, Cheques de sólo lectura para Supervisora CC y Rendiciones restringidas a Tesorería/Admin. Incorporado `admin/usuarios.php` con alta, cambio de rol/estado lógico y reset Bcrypt auditado; IDs de Google Sheets visibles/editables exclusivamente por Administrador. Las APIs administrativas aplican permisos granulares y respuestas JSON seguras.
- [x] **Módulo 3 — Fases 1 y 2 (Base de Datos y Backend)** — Incorporadas las cuatro tablas de Rendiciones, migración productiva idempotente para phpMyAdmin, bolsa de borradores, hash antifraude, consolidación transaccional contra presupuestos mensuales/giras, Magic Token de un solo uso, APIs de Tesorería, aprobación parcial, recepción física y auditoría.
- [x] **Módulo 3 — Fases 3 y 4 (Interfaces Vendedor y Tesorería)** — Implementada la vista móvil integrada `rendiciones/vendedor.php` con KPIs, carga progresiva, peaje rápido, Cena Cliente SII, bolsa seleccionable, semáforo de presupuesto e historial. `admin/rendiciones.php` incorpora sidebar con Bandeja, Dashboard y Vendedores; la Bandeja replica el patrón de Cheques Cobranza con píldoras, filtros, tabla maestra y detalle lateral, mientras Dashboard y Vendedores concentran analítica y presupuestos/giras. Mantiene Lightbox compartido, transiciones auditables, aprobación parcial e integración con los portales productivo y DEV. SQL nuevo: ninguno.
- [x] **Ajuste operativo de Bandeja Rendiciones** — La sidebar administrativa se puede contraer/expandir en escritorio y la “Bandeja por revisar” consolida revisión normal, excesos y documentos físicos recibidos. Los casos de exceso se distinguen por badge de monto y estado semántico en cada fila; no se modificaron APIs ni esquema SQL.
- [x] **Laragon local listo para Rendiciones** — Aplicada `config/setup_rendiciones.sql` en la BD local, integridad de 14 tablas y 12 pruebas funcionales aprobadas. La raíz opera con variables locales y `dist/` conserva la configuración productiva mediante validación diferenciada en `scripts/verify_release.php`.
- [x] **Nota del vendedor a Tesorería** — La observación ingresada al consolidar una rendición se persiste en la cabecera, queda trazada en `rendicion_historial_estados` y se presenta en el detalle administrativo. La columna cuenta con migración local/productiva aditiva documentada.
- [x] **E2E Rendiciones local** — Flujo comprobado con datos de prueba trazables: sesión vendedor, Peaje con fotografía, presupuesto excedido, nota a Tesorería, aprobación por Magic Token de uso único, recepción física y aprobación total. La cuenta administrativa temporal quedó desactivada lógicamente; la rendición aprobada se conserva como evidencia de prueba local.
- [x] **Portal de Rendiciones adaptado para tablet** — Desde 768 px el acceso de vendedor usa el ancho útil de tablet, conserva controles táctiles y abre formularios/rendiciones en drawers centrados de dos columnas. La validación en iPad Pro (1024 × 1366) confirmó drawer de 922 px, grillas activas y cero errores de consola; a 390 px se conserva el layout móvil de una columna.
- [x] **Selectores de gasto modernizados** — Categoría y Tipo de Documento ahora usan un selector Vanilla accesible, con menú táctil alineado al lenguaje visual de Rendiciones, navegación por teclado y cierre por Escape/clic exterior. Conserva los `select` nativos ocultos para mantener los contratos de formulario y la selección automática de Peaje.
- [x] **Vista tablet de Rendiciones refinada** — Entre 600 y 1199 px, gastos, informes y presupuestos utilizan grillas híbridas `auto-fit`: una única tarjeta ocupa el ancho útil y varias se distribuyen en dos columnas. Se reforzaron tamaños de tarjetas, miniaturas, métricas, filtros, FAB y navegación inferior para lectura y toque cómodos.
- [x] **Presupuesto vendedor reformado por crítica Impeccable** — La vista deja el patrón de KPI genérico: ahora muestra asignado, utilizado, saldo y porcentaje de uso con jerarquía financiera. Corrige el contrato visual para usar `monto_utilizado`, y un saldo negativo se identifica como “Exceso comprometido” en rojo, nunca como disponible verde. Sin cambios de BD ni APIs.
- [x] **Identidad ERP en presupuestos administrativos** — Tesorería selecciona empresa y vendedor desde los catálogos ERP reales; nombre, correo y código quedan bloqueados contra escritura libre y se revalidan en backend antes de persistir. El directorio muestra la homologación por correo y los códigos locales por empresa. Un presupuesto mensual permite agregar una gira sin volver a capturar la identidad. Validación local: 19 comprobaciones con rollback completo. SQL nuevo: ninguno.
- [x] **Presupuesto vendedor con resolución real** — El cupo mensual y las giras distinguen monto aprobado, monto pendiente de Tesorería y saldo disponible. La barra segmentada evita presentar una rendición en revisión como gasto aprobado, mientras el pendiente continúa reservado contra doble uso. SQL nuevo: ninguno.
- [x] **Bloqueo de envío sin presupuesto** — La interfaz detiene la consolidación cuando el vendedor no posee un presupuesto activo y le indica solicitarlo a Gerencia. Los avisos de exceso ya no exponen el nombre de la persona aprobadora. SQL nuevo: ninguno.
- [x] **Aprobación de excesos configurable** — El portal permite a un Administrador mantener dos responsables (nombre, cargo y correo); Tesorería selecciona el destinatario al emitir o reenviar la solicitud. El correo y la página Magic Token entregan el contexto financiero, comprobantes y datos SII antes de resolver. Requiere importar `config/migrations/2026_08_26_aprobadores_rendiciones.sql` en cada base desplegada.
- [x] **Migración y QA local de aprobadores** — La migración 2026-08-26 quedó aplicada e idempotente en Laragon: tabla, seis columnas snapshot y restricciones verificadas; 28 comprobaciones de Rendiciones superadas, CSRF negativo HTTP 403 y paridad root/dist certificada.
- [x] **Configuraciones separadas sin regresión** — El botón global “Configuración” conserva el panel histórico de Cuentas Corrientes (corte, correos, digitadoras, empresas y Google Sheets). En Rendiciones, “Aprobadores” abre separadamente la configuración de Gerencia/Jefatura, también disponible desde Vendedores.
- [x] **Modal global de configuración visible** — Corregida la colisión entre `hidden` y `display:none` que impedía mostrar el panel compartido aunque sus datos cargaran correctamente. Los tres portales referencian la versión CSS renovada.
- [x] **Comprobante PDF de exceso aprobado** — La resolución Magic Token queda cerrada visualmente tras la decisión y Tesorería puede abrir/imprimir una constancia con resumen, comprobantes, fecha, código de verificación y firma textual (nombre/cargo) del responsable seleccionado. El documento certifica el exceso, no el pago final. SQL nuevo: ninguno.
- [x] **PDF optimizado para impresión B/N** — La constancia elimina cabeceras y bloques con tinta sólida; utiliza blanco, negro, grises, bordes finos y jerarquía tipográfica para conservar legibilidad con bajo consumo de tóner.
- [x] **Rechazo preventivo de excesos por Tesorería** — Una rendición con exceso puede cancelarse antes de escalarla; exige motivo, libera fondos, invalida enlaces existentes y conserva auditoría sin enviar correo nuevo. SQL nuevo: ninguno.
- [x] **Recarga interna del Shell y Dashboard financiero corregido** — Los módulos pueden actualizar datos sin recargar la sesión. Rendiciones mide exclusivamente montos efectivamente aprobados/pagados y excluye rechazados de KPIs, categorías y comparativas. SQL nuevo: ninguno.
- [x] **Analítica histórica de vendedores** — El Dashboard compara 6/12 meses de presupuesto, aprobado, pendiente y fricción por identidad ERP/empresa. Incluye detalle mensual seleccionable, ticket promedio y señales accionables de concentración, baja ejecución, excesos recurrentes, rechazos y cupos próximos al límite. El escenario local demostrativo es reproducible e idempotente; no requiere migración SQL.
- [x] **Giras comerciales endurecidas** — Alta administrativa con período derivado desde la fecha inicial y validaciones específicas; comprobantes restringidos al rango del viaje; saldo, exceso, correo y auditoría asociados al fondo de gira seleccionado. El Dashboard compara Mensual/Gira mediante métricas estandarizadas y omite nombres libres. Demo local: 36 fondos mensuales + 3 giras; 38 pruebas PASS. SQL nuevo: ninguno.

## Próximo trabajo inmediato

1. **Nueva política de Rendiciones — cierre productivo:** importar la migración, desplegar `dist` y ejecutar un smoke test SMTP controlado con destinatarios autorizados. Las Fases A–H y la certificación local de Fase I están completas.
2. **Módulo 3 — Fase 5:** importar `config/migrations/2026_08_26_aprobadores_rendiciones.sql`, configurar los dos responsables desde el portal y ejecutar un smoke test SMTP controlado del flujo de exceso.
2. **Subida y Prueba Interna en Host:** Desplegar el sistema al host de pruebas (en la nube usando el directorio `dist/` o la raíz).
   - *Importante:* Recordar editar el archivo `.htaccess` subido con los datos reales de BD.
2. Probar el flujo completo en ambiente de pruebas (Tesorería validando y modificando cobranzas).
3. **Paso a Producción Final (Correos SMTP):**
   - Actualmente existe una **Barrera de Seguridad (Safety Guard)** en `services/MailService.php` que intercepta todos los correos si la variable de entorno `APP_ENV` es igual a `local`.
   - Con el nuevo `.htaccess` que setea `APP_ENV production`, los correos empezarán a salir una vez configurado el `MAIL_PASS`.
4. Configurar las 3 tareas en el Crontab del servidor Linux según las instrucciones detalladas en `cron/README.md`.

